# PyTorch CPU Index (prevents heavy GPU allocations and startup OOM)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchvision==0.17.2+cpu

# Core numeric & imaging (pinned to 1.x to avoid PyTorch binary conflict)
numpy>=1.26.0,<2.0.0
pillow

# Server & Headless OpenCV
fastapi
uvicorn
sqlalchemy
aiofiles
python-multipart
opencv-python-headless==4.10.0.84